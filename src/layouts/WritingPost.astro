---
import type { CollectionEntry } from "astro:content";
import FormattedDate from "../components/FormattedDate.astro";
import Header from "../components/Header.astro";
import Layout from "./Layout.astro";
import SiteFooter from "../components/SiteFooter.astro";

type Props = CollectionEntry<"writing">["data"];

const { title, description, pubDate, updatedDate } = Astro.props;
---

<Layout title={title} description={description}>
  <div class="min-h-dvh">
    <Header />
    <main class="px-page-x pt-page-y-lg pb-24">
      <article class="mx-auto max-w-reading">
        <header class="mb-14 sm:mb-20">
          <p class="post-meta">
            <FormattedDate date={pubDate} class="post-meta__date" />
            {
              updatedDate && (
                <span class="post-meta__updated">
                  · Updated <FormattedDate date={updatedDate} class="post-meta__date" />
                </span>
              )
            }
          </p>
          <h1 class="post-title">{title}</h1>
          {
            description && (
              <p class="post-dek">{description}</p>
            )
          }
        </header>
        <div class="prose">
          <slot />
        </div>
      </article>
    </main>
  </div>
  <SiteFooter flat />
</Layout>

<style>
  .post-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
    font-family: var(--font-sans);
    font-size: 0.75rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .post-meta :global(.post-meta__date) {
    font-size: inherit;
    letter-spacing: inherit;
    text-transform: inherit;
    color: inherit;
  }

  .post-meta__updated {
    color: var(--color-muted);
  }
</style>
